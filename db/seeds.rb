# Seed data is intentionally empty.
#
# The previous seed file referenced a Title model/table that does not exist in
# this application, causing `bin/rails db:seed` and `db:setup` to fail.
# Add only records backed by current application models here.
